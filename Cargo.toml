[package]
name = "binary-format-workbench"
version = "0.1.0"
edition = "2021"

[lib]
name = "bfw"
path = "src/lib.rs"

[[bin]]
name = "server"
path = "src/bin/server.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "unwind"
